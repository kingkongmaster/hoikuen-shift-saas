import { useState } from 'react';
import { api, type BackupValidation, type PrintShiftData, type Session } from '../../api/client';
import { MessageBanner } from '../../components/UiStates';
import { currentMonthKey } from '../shifts/month-utils.js';

const save = ({ blob, name }: { blob: Blob; name: string }) => { const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = name; link.click(); URL.revokeObjectURL(url); };
const escape = (value: unknown) => String(value ?? '').replace(/[&<>]/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;' }[char]!));
const classLabels: Record<string, string> = { AGE_0: '0歳児', AGE_1: '1歳児', AGE_2: '2歳児', AGE_3: '3歳児', AGE_4: '4歳児', AGE_5: '5歳児', FREE: 'フリー', SUPPORT: '補助' };
function print(data: PrintShiftData, classFilter = '') {
  const filterLabel = classLabels[classFilter] ?? classFilter;
  const assignments = classFilter ? data.assignments.filter((item) => item.assignedClass === filterLabel) : data.assignments;
  const closed = new Map(data.closedDates.map((item) => [item.date, item.name]));
  const rows = assignments.map((item) => `<tr><td>${escape(item.staffName)}</td><td>${escape(item.date)}</td><td>${escape(item.weekday)}</td><td>${escape(item.shiftType)}</td><td>${escape(item.assignedClass)}</td><td>${escape(item.startTime ? `${item.startTime}〜${item.endTime ?? ''}` : '')}</td><td>${escape(closed.get(item.date) ?? '')}</td></tr>`).join('');
  const popup = window.open('', '_blank');
  if (!popup) throw new Error('印刷画面を開けませんでした。ブラウザのポップアップを許可してください。');
  popup.opener = null;
  const scope = data.ownOnly ? '個人' : classFilter ? `${filterLabel} クラス別` : '園全体';
  popup.document.write(`<!doctype html><html lang="ja"><head><title>EnShift ${escape(scope)}月間シフト</title><style>@page{size:A4 landscape;margin:10mm}body{font-family:sans-serif;font-size:11px;color:#0f172a}h1{color:#047857}table{width:100%;border-collapse:collapse}th,td{border:1px solid #64748b;padding:4px;text-align:left}th{background:#f1f5f9}@media print{button{display:none}}</style></head><body><h1>${escape(data.tenantName)} ${escape(scope)}月間シフト</h1><p>${escape(data.month)}／${data.status === 'CONFIRMED' ? '確定済み' : '下書き'}／出力 ${escape(new Date(data.printedAt).toLocaleString('ja-JP'))}</p><p>凡例：早=EARLY、通=NORMAL、遅=LATE、休=OFF</p><table><thead><tr><th>職員名</th><th>日付</th><th>曜日</th><th>勤務区分</th><th>配置クラス</th><th>勤務時間</th><th>休園日</th></tr></thead><tbody>${rows}</tbody></table><button onclick="window.print()">印刷／PDFとして保存</button></body></html>`);
  popup.document.close();
}

export function DataExportManagement({ session }: { session: Session }) {
  const [month, setMonth] = useState(currentMonthKey()); const [classFilter, setClassFilter] = useState('AGE_0'); const [message, setMessage] = useState(''); const [messageError, setMessageError] = useState(false); const [backup, setBackup] = useState<Record<string, unknown> | null>(null); const [validation, setValidation] = useState<BackupValidation | null>(null); const [preview, setPreview] = useState<any>(null); const busy = async (label: string, action: () => Promise<void>) => { setMessageError(false); setMessage(`${label}中…`); try { await action(); setMessage(`${label}が完了しました。`); } catch (error) { setMessageError(true); setMessage(error instanceof Error ? error.message : `${label}を完了できませんでした。時間をおいてもう一度お試しください。`); } };
  const readBackup = async (file: File) => { if (file.size > 10 * 1024 * 1024) throw new Error('バックアップファイルは10MB以下にしてください。'); const parsed = JSON.parse(await file.text()); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('JSONファイルを選択してください。'); setBackup(parsed as Record<string, unknown>); setValidation(null); setPreview(null); };
  return <section className="mt-6 space-y-5"><div className="card"><h3 className="font-bold">月別データ・PDF</h3><div className="mt-3 flex flex-wrap gap-2"><input aria-label="出力対象月" type="month" value={month} onChange={(event) => setMonth(event.target.value)} className="input max-w-48" /><button type="button" onClick={() => void busy('月間シフトCSV出力', async () => save(await api.downloadShiftCsv(session.accessToken, month)))} className="btn-primary">月間シフトCSV</button><button type="button" onClick={() => void busy('希望休CSV出力', async () => save(await api.downloadRequestsCsv(session.accessToken, month)))} className="btn-secondary">希望休CSV</button><button type="button" onClick={() => void busy('園全体印刷画面表示', async () => print(await api.printShifts(session.accessToken, month)))} className="btn-secondary">園全体 印刷／PDF</button><button type="button" onClick={() => void busy('個人印刷画面表示', async () => print(await api.printShifts(session.accessToken, month, true)))} className="btn-secondary">個人 印刷／PDF</button></div><div className="mt-3 flex flex-wrap gap-2"><select aria-label="印刷対象クラス" value={classFilter} onChange={(event) => setClassFilter(event.target.value)} className="input max-w-48"><option value="AGE_0">0歳児</option><option value="AGE_1">1歳児</option><option value="AGE_2">2歳児</option><option value="AGE_3">3歳児</option><option value="AGE_4">4歳児</option><option value="AGE_5">5歳児</option><option value="FREE">フリー</option><option value="SUPPORT">補助</option></select><button type="button" onClick={() => void busy('クラス別印刷画面表示', async () => print(await api.printShifts(session.accessToken, month), classFilter))} className="btn-secondary">クラス別 印刷／PDF</button></div></div><div className="card"><h3 className="font-bold">マスター・履歴CSV</h3><div className="mt-3 flex flex-wrap gap-2"><button type="button" onClick={() => void busy('職員CSV出力', async () => save(await api.downloadStaffCsv(session.accessToken)))} className="btn-secondary">職員一覧CSV</button><button type="button" onClick={() => void busy('監査ログCSV出力', async () => save(await api.downloadAuditCsv(session.accessToken)))} className="btn-secondary">監査ログCSV</button></div></div><div className="card"><h3 className="font-bold">バックアップ</h3><p className="mt-2 text-sm text-slate-600">バックアップには認証情報を含めません。JSON Importは内容の検証と復元プレビューまで行い、現在のデータは変更しません。</p><div className="mt-3 flex flex-wrap gap-2"><button type="button" onClick={() => void busy('バックアップJSON Export', async () => save(await api.exportBackup(session.accessToken)))} className="btn-primary">JSON Export</button><label className="btn-secondary">JSON Import<input className="sr-only" type="file" accept="application/json,.json" onChange={(event) => { const file = event.target.files?.[0]; if (file) void busy('JSON Import', () => readBackup(file)); }} /></label>{backup && <><button type="button" onClick={() => void busy('バックアップ検証', async () => setValidation(await api.validateBackup(session.accessToken, backup)))} className="btn-secondary">バックアップ検証</button><button type="button" onClick={() => void busy('復元プレビュー', async () => setPreview(await api.previewBackup(session.accessToken, backup)))} className="btn-secondary">復元プレビュー</button></>}</div>{validation && <div className="message-banner message-success mt-4"><strong>有効</strong>／version {validation.metadata.version}／{validation.metadata.tenantName}<pre className="mt-2 overflow-auto">{JSON.stringify(validation.counts, null, 2)}</pre></div>}{preview && <div className="message-banner message-warning mt-4"><strong>これは復元内容の確認画面です。現在のデータは変更されません。</strong><pre className="mt-2 overflow-auto">{JSON.stringify(preview.preview, null, 2)}</pre></div>}</div>{message && <MessageBanner kind={messageError ? 'error' : 'info'}>{message}</MessageBanner>}</section>;
}
