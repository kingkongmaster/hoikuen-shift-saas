import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const client=await readFile(new URL('../src/api/client.ts',import.meta.url),'utf8');const page=await readFile(new URL('../src/features/staff/StaffAttributeManagement.tsx',import.meta.url),'utf8');const staff=await readFile(new URL('../src/features/staff/StaffManagement.tsx',import.meta.url),'utf8');assert.match(client,/StaffAttributeDefinition/);assert.match(client,/\/staff-attributes/);assert.match(client,/\/attributes/);assert.match(page,/ROLE_QUALIFICATION_MANAGEMENT/);assert.match(page,/現在のプランでは属性の追加・編集・無効化は利用できません/);assert.match(page,/属性無効/);assert.match(page,/システム属性/);assert.match(page,/\['ROLE','ASSIGNMENT'\]\.includes/);assert.match(page,/既存主属性の期間を調整してから登録してください/);assert.match(page,/break-words/);assert.match(page,/現在の自動生成は従来の職員項目を使用/);assert.doesNotMatch(page,/dangerouslySetInnerHTML/);assert.match(staff,/StaffAttributeManagement/);console.log('StaffAttribute Web tests: PASS');
